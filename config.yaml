# ============================================================
# ML Framework Comparison - 전역 설정
# ============================================================

# MLflow 서버 설정
mlflow:
  tracking_uri: "https://temperamental-kazuko-enamouredly.ngrok-free.dev/"
  experiment_name: "cifar100-framework-comparison-1"
  artifact_location: "./mlruns/artifacts"
  run_names:
    sklearn: 'sklearns_mlp_batch256_epoch50'
    tensorflow: 'tensorflow_resnet_batch256_epoch50'
    flax: 'flax_resnet_batch256_epoch50'

# 데이터셋 설정
data:
  dataset: "cifar100"
  num_classes: 100
  data_dir: "./data"
  val_split: 0.2
  random_seed: 42

# 학습 하이퍼파라미터 (세 프레임워크 공통)
train:
  epochs: 50
  batch_size: 256
  learning_rate: 0.001
  dropout_rate: 0.3
  l2_lambda: 0.0001
  optimizer: "adam"

# 모델 아키텍처 설정
model:
  model_type: "ResNet"
  flax_module: 'flax_resnet_model'                # models/flax_*_model.py
  tensorflow_module: 'tensorflow_resnet_model'    # models/tensorflow_*_model.py
  sklearn_module: 'sklearn_mlp_model'             # models/sklearn_*_model.py

  input_shape: [32, 32, 3]
  filters: [32, 64, 128]
  dense_units: [512, 256]

# 프레임워크별 설정
frameworks:
  sklearn:
    enabled: true
    solver: "adam"
    max_iter: 50
    early_stopping: true
    n_iter_no_change: 5
    tol: 0.0001

  tensorflow:
    enabled: true
    mixed_precision: false
    lr_scheduler: "none"

  flax:
    enabled: true
    use_jit: true
    xla_flags: "default"
    jax_backend: "gpu"        # jax.default_backend()로 자동 감지

# 로깅 설정
logging:
  save_artifacts: true
  artifact_dir: "./artifacts"
  top5: true
  track_gpu: true